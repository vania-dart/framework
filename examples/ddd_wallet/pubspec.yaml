name: ddd_wallet
description: A small Domain-Driven Design sample (wallet domain) built with the Vania framework.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  vania:
    path: ../../packages/core

dependency_overrides:
  vania:
    path: ../../packages/core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
