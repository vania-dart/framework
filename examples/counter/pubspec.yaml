name: counter
description: A minimal in-memory counter API built with the Vania framework.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  vania:
    path: ../../packages/core

dependency_overrides:
  vania:
    path: ../../packages/core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
